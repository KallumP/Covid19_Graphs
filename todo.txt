turn things like point size into a static class called settings

The default data should search for the country in the list of all countries
and if that search did not return null, add that country


color picker for graph color
need to wait until the user clicks choose before adding the country to the graph
maybe use bool variables
the select method is on a while loop until the window's bool method is true
after the method, set it back to false

turn the color buttons into programatic